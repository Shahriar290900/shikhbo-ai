# torch comes from the base image (pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime)
# DO NOT add torch here — it would reinstall a CPU-only build and break CUDA.
fastapi
uvicorn[standard]
transformers>=4.51.0
accelerate
qwen-vl-utils
Pillow
python-multipart
